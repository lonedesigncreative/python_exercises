# Exercise description:

- Develop a program that **asks the user to enter three decimal numbers**.
- After reading the three values, the program should **calculate and display the average of the numbers entered**.
- **Note:** Use the `input()` function to read each value and convert the text input into a decimal number using the `float()` function.