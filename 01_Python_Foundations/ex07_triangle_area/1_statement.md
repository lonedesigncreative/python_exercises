# Exercise description:

- Create a program that **asks the user to enter the base and height of a right triangle**.
- Using these two values, the program should c**alculate the area of the triangle** and **display the result to the user**.
- **Note:** Use the `input()` function to read the values and convert the text input into decimal numbers using the `float()` function.