> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `print("=== MAIN MENU ===")`
> - Display the main menu header to the user, indicating that the following options are part of the main menu for product management
> 
> 2. `print("1 - Add Product")`  
> - Option 1 allows the user to add a new product to the product list, which will be implemented in the code block that handles this option
>
>
> 3. `print("2 - Update Product")` 
> - Option 2 allows the user to update the details of an existing product in the product list, which will be implemented in the code block that handles this option
> 
> 4. `print("3 - Delete Product")` 
> - Option 3 allows the user to delete a product from the product list, which will be implemented in the code block that handles this option
> 
> 5. `print("4 - View Product List")`
> - Option 4 allows the user to view the list of products currently in the product list, which will be implemented in the code block that handles this option
> 
> 6. `print("5 - Search Product")`
> - Option 5 allows the user to search for a specific product in the product list based on certain criteria (e.g., product name, category, etc.), which will be implemented in the code block > that handles this option    
> 
> 7. `print("6 - Exit")`
> - Option 6 allows the user to exit the product management program, which will be implemented in the code block that handles this option