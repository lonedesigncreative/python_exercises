# Exercise description:

- **Ask the user to enter three integer numbers**.
- After reading the values, **use a decision structure** to determine **which of the three numbers is the largest**.
- The program should **compare the three values** and then **display** a message indicating which one is the highest.
- **If** some of the values are equal, the program should still identify the largest number among the three, regardless of ties.