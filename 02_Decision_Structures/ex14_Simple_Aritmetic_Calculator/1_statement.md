# Exercise description:

- Develop a program that:
    - Asks the user to choose an **arithmetic operation**: `+`, `-`, `*`, or `/`.
    - Requests two numerical values.
    - Performs the selected operation.
    - If the user chooses **division** and the denominator is **0**, displays a custom error message.
    - Otherwise, shows the **result** of the chosen operation.