> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `for i in range(100):`
> - Starts a loop that runs 100 times, with `i` going from `0` to `99`.
>
> 2. `print("hello")`
> - Each time through the loop, prints `"hello"`.