> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `for i in range(1, 101):`
> - Starts a loop with `i` taking values from `1` to `100` (inclusive).
>
> 2. `print(i, i**2)`
> - Prints `i` and `i**2` (i squared) on the same line.