> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `for i in range(1, 11):`
> - Starts a loop with `i` from `1` to `10`.
>
> 2. `print(f"5 x {i} = {5 * i}")`
> - Prints a formatted string showing the multiplication `5 x i` and its result.