> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `number = int(input("Enter the number for the multiplication table: "))`
> - Reads an integer from the user and stores it in `number`.
>
> 2. `for i in range(1, 11):`
> - Starts a loop with `i` from `1` to `10`.
>
> 3. `print(f"{number} x {i} = {number * i}")`
> - Prints the multiplication of `number` by `i` and its result.