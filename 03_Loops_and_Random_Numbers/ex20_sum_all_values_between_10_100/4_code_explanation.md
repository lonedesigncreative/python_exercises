> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `total = 0`
> - Initializes `total` to `0`.
>
> 2. `for i in range(10, 101):`
> - Starts a loop with `i` from `10` to `100` (inclusive).
>
> 3. `total += i`
> - Adds `i` to `total`.
>
> 4. `print(f"The sum of the values between 10 and 100 is {total}")`
> - Prints the final sum.