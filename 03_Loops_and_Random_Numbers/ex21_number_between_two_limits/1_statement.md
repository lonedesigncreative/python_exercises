# Exercise description:

## Numbers Between Two Limits (Ascending Order)

- Write a program that:
    - Asks the user for **two integers**.
    - Displays all integers **between those two values**, in ascending order.
    - Uses a **`for` loop**.