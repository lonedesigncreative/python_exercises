> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `for i in range(1, 21):`
> - Starts a loop with `i` from `1` to `20`.
>
> 2. `print(5 * i)`
> - Prints the product of `5` and `i`, which is the `i`‑th multiple of 5.