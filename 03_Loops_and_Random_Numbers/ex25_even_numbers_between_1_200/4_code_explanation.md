> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `for i in range(1, 201):`
> - Starts a loop with `i` from `1` to `200` (inclusive).
>
> 2. `if i % 2 == 0:`
> - Checks if `i` is even by testing if the remainder of `i` divided by `2` is `0`.
>
> 3. `print(i)`
> - If `i` is even, prints its value.