> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `colors = ["yellow", "blue", "white", "black", "green"]`
> - Creates a list named `colors` with five string elements.
> 
> 2. `print(colors)`
> - Prints the entire `colors` list.
> 
> 3. `print(colors[2])` 
> - Prints the element at index `2` of the list, which is the third element.
> 
> 4. `colors[0] = "red"` 
> - Replaces the element at index `0` with the string `"red"`.
>
> 1. `print(colors)`
> - Prints the updated `colors` list.
> 
> 2. `colors.append("yellow")`
> - Adds the string `"yellow"` to the end of the list.
> 
> 3. `print(colors)` 
> - Prints the updated list with the new element at the end.
> 
> 4. `colors.insert(2, "purple")` 
> - Inserts the string `"purple"` at index `2`, shifting the other elements to the right.
>
> 1. `print(colors)`
> - Prints the list with `"purple"` inserted.
> 
> 2. `colors.pop()`
> - Removes and returns the last element of the list.
> 
> 3. `print(colors)` 
> - Prints the list after removing the last element.
> 
> 4. `Prints the list after removing the last element.` 
> - Prints the number of elements currently in the `colors` list.