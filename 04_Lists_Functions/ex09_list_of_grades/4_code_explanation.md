> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `grades = [11.2, 15, 8.7, 17.2, 7.9]`
> - Creates a list named `grades` with five numeric values.
> 
> 2. `grades.append(10.9)`
> - Adds the value `10.9` to the end of the `grades` list.
> 
> 3. `print(grades)` 
> - Prints the full `grades` list.
> 
> 4. `print(len(grades))` 
> - Prints how many elements are in the `grades` list.
>
> 1. `average = sum(grades) / len(grades)`
> - Calculates the average by summing all values in `grades` and dividing by the number of elements, then stores the result in `average`.
> 
> 2. `print(average)`
> - Prints the value stored in `average`.