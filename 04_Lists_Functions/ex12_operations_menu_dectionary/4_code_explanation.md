> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `menu = {`
`"Pork Belly": 7,`
`"Sardine": 6,`
`"Filets": 5.5,`
`"Steak Sandwich": 7,`
`"Hamburger": 5.5`
`}`
> - Creates a dictionary named `menu` where each key is a dish name and each value is its price.
> 
> 2. `print(menu["Steak Sandwich"])`
> - Accesses the value associated with the key `"Steak Sandwich"` and prints it.
> 
> 3. `print(menu.keys())` 
> - Retrieves all the keys of the dictionary and prints them.
> 
> 4. `menu["Omelette"] = 5` 
> - Adds a new key `"Omelette"` with value 5 to the dictionary.
>
> 5. `menu["Omelette"] = 5` 
> - Prints the entire `menu` dictionary with the new entry.