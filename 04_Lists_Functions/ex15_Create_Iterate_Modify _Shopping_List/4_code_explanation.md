> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `shopping = ['milk', 'bread', 'eggs', 'butter', 'coffee']`
> - Creates a list named `shopping` with five string items.
> 
> 2. `print("Original list:", shopping)`
> - Prints the text `"Original list:"` followed by the full list.
> 
> 3. `for item in shopping:` 
> - Starts a loop that goes through each element in the list, assigning each one to `item`.
> 
> 4. `print("Item:", item)` 
> - Prints the word `"Item:"` and the current element of the loop.
>
> 5. `hopping[1] = 'wholegrain bread'`
> - Replaces the element at index `1` with the new value `'wholegrain bread'`.
> 
> 6. `print("Updated list:", shopping)`
> - Prints the text `"Updated list:"` and the modified list.