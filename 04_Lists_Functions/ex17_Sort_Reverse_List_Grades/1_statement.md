# Exercise description:

## Sort and Reverse a List of Grades

- Sort lists using:
  - `sorted()` → creates a **new** sorted list.
  - `sort()` → sorts the **original** list in place.
- Reverse the order of elements using `reverse()`.
- Understand the difference between modifying the original list and creating a new one.