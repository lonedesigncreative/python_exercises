# Exercise description:

## Filter and Transform Using List Comprehension

- Create new lists using list comprehension.
- Apply transformations to each element.
- Filter elements using conditions (`if`).
- Combine filtering and transformation in a single expression.