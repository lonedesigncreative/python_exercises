> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `def Show_Name(name, age):`
> - Define a function called Show_Name that takes two parameters: name and age. These will be used inside the function.
> 
> 2. `print(f"My name is {name} and I am {age} years old.")`
> - Inside the function, this line prints a sentence using an f-string, inserting the values of name and age into the text.
> 
> 3. `Show_Name("Michael", 25)` 
> - Calls the function Show_Name and passes "Michael" as the name and 25 as the age, so it prints a sentence with those values.
> 
> 4. `Show_Name("Sophie", 32)` 
> - Calls the same function again, now with "Sophie" and 32, printing a new sentence with these values.
>
> 5. `Show_Name("Leonard", 41)` 
> - Calls the function a third time, with "Leonard" and 41, printing the corresponding sentence.