> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `def Sum_Numbers(num1, num2):`
> - Defines a function called Sum_Numbers that receives two parameters: num1 and num2.
> 
> 2. `total = num1 + num2`
> - Creates a variable total and stores the result of adding num1 and num2.
> 
> 3. `print(f"Sum: {total}")` 
> - Prints the text "Sum: " followed by the value stored in total, using an f-string.
> 
> 4. `n1 = float(input("Enter number 1: "))` 
> - Asks the user to type the first number, converts the input to a float, and stores it in the variable n1.
>
> 5. `n2 = float(input("Enter number 2: "))` 
> - Asks the user to type the second number, converts the input to a float, and stores it in the variable n2.
>
> 6. `Sum_Numbers(n1, n2)` 
> - Calls the function Sum_Numbers, passing n1 and n2 as arguments, so the function can add them and print the result.