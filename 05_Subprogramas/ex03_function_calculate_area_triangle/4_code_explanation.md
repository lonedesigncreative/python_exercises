> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `def Calculate_Triangle_Area(base, height):`
> - Defines a function called Calculate_Triangle_Area that receives two parameters: base and height, which represent the dimensions of the triangle.
> 
> 2. `area = (base * height) / 2`
> - Calculates the area of the triangle using the formula **area = base ⋅ height / 2** and stores the result in the variable `area`.
> 
> 3. `print(f"Triangle area: {area}")` 
> - Prints the text `"Triangle area: "` followed by the value stored in area, using an f-string.
> 
> 4. `Calculate_Triangle_Area(4, 7.2)` 
> - Calls the function `Calculate_Triangle_Area` with `base = 4` and `height = 7.2`, and prints the corresponding area.
>
> 5. `Calculate_Triangle_Area(9, 10)` 
> - Calls the function again, this time with `base = 9` and `height = 10`.
>
> 6. `Calculate_Triangle_Area(2, 52)` 
> - Calls the function a third time, with `base = 2` and `height = 52`.