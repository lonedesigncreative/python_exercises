> [!NOTE]
> 
> # CODE EXPLANATION:
> 
> 1. `def Calculate_Average_Grades(grade1, grade2, grade3):`
> - Defines a function named Calculate_Average_Grades that receives three parameters: `grade1`, `grade2`, and `grade3`, which represent the three grades.
> 
> 2. `average = (grade1 + grade2 + grade3) / 3`
> - Calculates the arithmetic average of the three grades by adding them and dividing the sum by 3, then stores the result in the variable `average`.
> 
> 3. `print(f"The average of the three grades is: {average}")` 
> - Prints the text `"The average of the three grades is: "` followed by the value stored in `average`, using an f-string.
> 
> 4. `Calculate_Average_Grades(20, 12, 16)` 
> - Calls the function `Calculate_Average_Grades` with the values 20, 12, and 16 as the three grades, and prints the corresponding average.
>
> 5. `Calculate_Average_Grades(10, 15, 9)` 
> - Calls the function again with the grades 10, 15, and 9.
>
> 6. `Calculate_Average_Grades(15, 11, 8)` 
> - Calls the function a third time with the grades 15, 11, and 8.