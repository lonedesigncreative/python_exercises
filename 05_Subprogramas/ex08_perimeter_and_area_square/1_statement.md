# Exercise description:

## Perimeter and Area of a Square

- Write a Python function that:
    - Receives the **side length** of a square.
    - Calculates the **perimeter** using:
      - perimeter = 4 × side
    - Calculates the **area** using:
      - area = side × side
    - Returns or prints both the **perimeter** and the **area**.