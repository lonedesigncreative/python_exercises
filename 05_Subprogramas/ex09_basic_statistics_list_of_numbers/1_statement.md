# Exercise description:

## Basic Statistics of a List of Numbers

- Write a Python function that:
  - Receives a **list of numbers**.
  - Computes the **sum** of all values.
  - Determines the **number of elements** in the list.
  - Calculates the **average** of the values.
  - Prints or returns the **sum**, **count**, and **average**.