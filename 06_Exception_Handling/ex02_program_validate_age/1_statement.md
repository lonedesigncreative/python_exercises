- Create a Python program that asks the user to enter their age.
- The program must attempt to convert the input into an integer.

- If the user enters a non‑numeric value, a ValueError should be raised.

- When this happens, the program must:
  - Catch the exception using a try/except structure
  - Display the following error message:
    - **"Error: Please enter a valid integer."**